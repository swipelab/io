fn add(a,b: i32) -> i32  => a + b;
fn scale(a,b: f32) -> f32  => a * b;

(2,3).add().scale(3)

fn intoData<T,R>(e: Span<T>) -> R => e.map(toData);


a = [td.ProductTile(..), td.ProductTile(..)];

c = a.map(XXX.toData);

c = a.fold(0, (acc, e) => acc + e.add());

scale(add(2,3))

namespace XXX {
  toData(td.ProductTile e) -> Product => {}
  toData(td.Category e) -> Product => {}
  toData(td.XXX e) -> Product => {}
}